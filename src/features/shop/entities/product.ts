export interface Product {
  name: string;
  image: string;
  category: string;
  id: string;
}

export interface ProductDetail {
  name: string;
  image: string;
  description: string;
  price: string;
  id: string;
}
